export type SubjectCol = {
    RDCode: string
    RDID: string
    RDDesc: string
}

export type PrerequisiteCol = {
    RDID: string
    RDDesc: string
    prerequisiteCode: string
    prerequisiteDesc: string
}